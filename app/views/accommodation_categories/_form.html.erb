<%= form_for(@accommodation_category) do |f| %>
  <% if @accommodation_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accommodation_category.errors.count, "error") %> prohibited this accommodation_category from being saved:</h2>

      <ul>
      <% @accommodation_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
